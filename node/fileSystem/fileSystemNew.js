//** === 3) Example === */
const fs = require(`fs`);

/* fs.readFile(__dirname + `./fileSystem.html`, `utf-8`, (err, container) => {
  if (err) {
    console.log(err);
  } else {
    console.log(container);
  }
}); */
